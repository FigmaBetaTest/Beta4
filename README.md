
  # Wording Objects Library Prototype

  This is a code bundle for the Wording Objects Library Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  